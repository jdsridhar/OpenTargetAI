# resources/

Static assets for OpenTargetAI (icons, logos, stylesheets) live here.

The application's dark theme is currently defined inline in `app.py`
(`load_stylesheet`). Drop additional `.qss` stylesheets, `.svg`/`.png` icons or
other bundled assets in this directory and reference them from the UI as the
application grows.
