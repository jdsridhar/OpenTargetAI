# OpenTargetAI — Python dependencies
# Tested on Python 3.12–3.14
# Install with:  pip install -r requirements.txt

# ── GUI ─────────────────────────────────────────────────────────────────────
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0

# ── Cheminformatics ─────────────────────────────────────────────────────────
rdkit>=2023.9.1

# ── Data processing & ML ────────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0          # ROC-AUC and validation metrics

# ── Visualization ───────────────────────────────────────────────────────────
pyqtgraph>=0.13.4            # in-application interactive charts & network
plotly>=5.18.0              # standalone interactive HTML dashboards
networkx>=3.2.1            # network layout

# ── Export / reporting ──────────────────────────────────────────────────────
openpyxl>=3.1.2             # Excel export
reportlab>=4.0.7           # PDF reports
jinja2>=3.1.2              # HTML reports

# ── Integration (PDB / docking) ─────────────────────────────────────────────
requests>=2.31.0           # RCSB / PDBe / ChEMBL REST access

# Database: SQLite3 is part of the Python standard library — no install needed.

# ── Optional extras ─────────────────────────────────────────────────────────
# pubchempy>=1.0.4          # fetch structures by name/CID from PubChem
# kaleido>=0.2.1            # static (PNG) export of Plotly figures
# tqdm>=4.66.0              # nicer CLI progress bars
